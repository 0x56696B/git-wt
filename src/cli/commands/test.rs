use super::TestArgs;

pub fn test_command(_args: TestArgs) -> Result<(), String> {
  // worktree.let
  // println!("WT: {:?}; Path: {:?}", worktree.name(), worktree.path());
  // // let (object, reference) = repo
  // //   .revparse_ext("feat/PPN-000-revert-disable-cerbos-for-slot-configs")
  // //   .map_err(|e| e.message().to_string())?;
  //
  // match reference {
  //   Some(reference) => {
  //     // Get the shorthand name of the reference
  //     let name = reference.shorthand().ok_or("No shorthand for reference")?.to_string();
  //     println!("Name: {:?}", name);
  //   }
  //   None => {
  //     println!("None");
  //   }
  // };
  return Ok(());
}
